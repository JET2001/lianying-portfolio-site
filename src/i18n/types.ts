export type Locale = 'en' | 'zh'

export interface UiStrings {
  sections: {
    selectedWork: string
    evidenceNotes: string
    education: string
    experience: string
    publications: string
    leadership: string
  }
  links: {
    resume: string
    email: string
    linkedin: string
    evidence: string
  }
  language: {
    switch: string
  }
  footer: {
    plainHtml: string
    noTracking: string
    lastUpdated: string
  }
}